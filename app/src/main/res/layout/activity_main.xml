<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">



    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:columnCount="3"
        android:rowCount="3"
        android:layout_marginTop="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Buttons for the Tic Tac Toe grid -->
        <Button android:id="@+id/button1" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button2" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button3" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button4" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button5" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button6" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button7" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button8" style="@style/GridButtonStyle" />
        <Button android:id="@+id/button9" style="@style/GridButtonStyle" />
    </GridLayout>

    <TextView
        android:id="@+id/statusTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Player X's Turn"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/gridLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_gravity="center" />

    <Button
        android:id="@+id/resetButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Reset Game"
        app:layout_constraintTop_toBottomOf="@+id/statusTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_gravity="center" />

</androidx.constraintlayout.widget.ConstraintLayout>
